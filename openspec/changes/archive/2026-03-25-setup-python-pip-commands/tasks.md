## 1. Detection and Setup

- [x] 1.1 Detect user's default shell (bash or zsh)
- [x] 1.2 Identify the correct shell configuration file path (~/.bashrc for bash, ~/.zshrc for zsh)

## 2. Add Aliases

- [x] 2.1 Add `python` alias pointing to `python3` to shell config
- [x] 2.2 Add `pip` alias pointing to `pip3` to shell config
- [x] 2.3 Add descriptive comment explaining the aliases

## 3. Verification

- [x] 3.1 Source the updated shell config
- [x] 3.2 Verify `python --version` returns Python 3 version
- [x] 3.3 Verify `pip --version` returns pip3 version